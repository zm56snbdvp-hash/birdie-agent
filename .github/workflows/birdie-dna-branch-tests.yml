name: Birdie DNA Branch Tests

on:
  push:
    branches:
      - feature/birdie-dna-v0
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    if: github.event_name != 'pull_request' || github.head_ref == 'feature/birdie-dna-v0'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Run test suite
        run: npm test
